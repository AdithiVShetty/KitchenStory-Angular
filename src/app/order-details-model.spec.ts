import { OrderDetailsModel } from './order-details-model';

describe('OrderDetailsModel', () => {
  it('should create an instance', () => {
    expect(new OrderDetailsModel()).toBeTruthy();
  });
});
